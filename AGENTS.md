## Agent Modification Constraints

For the hackathon submission, `agents/answer_agent.py` and `agents/question_agent.py` must remain unchanged aside from prompt text adjustments. Altering any other part of these files could invalidate our entry, so treat all non-prompt code as off limits.

## Workflow Note

Always review `git diff` before committing to confirm these files were not unintentionally modified.
